@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container mt-5">
    <div class="text-center mb-5">
        <h1 class="display-4">🎵 Academia de Música Kong Egg</h1>
        <p class="lead text-muted">Sistema de gestión</p>
    </div>

    <div class="row justify-content-center g-4">
        <div class="col-md-4">
            <div class="card text-center shadow">
                <div class="card-body p-4">
                    <h2>🎓</h2>
                    <h5 class="card-title mt-2">Alumnos</h5>
                    <p class="card-text text-muted">Gestión de alumnos de la academia</p>
                    <a href="/Alumnos" class="btn btn-success w-100">Gestionar</a>
                </div>
            </div>
        </div>
    </div>
</div>